// import "./Main.css";
import Admin from "@/Layouts/Admin";

const Main = (props) => {
    return (
        <div className=" w-full" style={{height:'2000px'}}>hello</div>
    );
};

Main.layout = (page) => (<Admin>{page}</Admin>);

export default Main;
